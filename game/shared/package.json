{
  "name": "@coin-pusher/shared",
  "version": "0.1.0",
  "description": "Shared types and constants for coin pusher game",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "buf:generate": "npx buf generate",
    "build": "pnpm buf:generate && tsc",
    "clean": "rm -rf dist",
    "dev": "tsc --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.11.0"
  },
  "devDependencies": {
    "@bufbuild/protoc-gen-es": "^2.11.0",
    "typescript": "^5.3.3",
    "vitest": "^4.0.18"
  }
}
